fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
google-api-python-client==2.149.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
httpx==0.27.2
aiosqlite==0.20.0
apscheduler==3.10.4
Pillow==11.3.0
pdfplumber==0.11.4
python-multipart==0.0.12
# Postgres driver (production). Local dev still uses SQLite per
# database_url default. psycopg[binary] avoids the libpq compile step
# on Render's build image — significantly faster cold-build times.
psycopg[binary]==3.2.3
# JWT verification for the Supabase auth middleware. The `crypto` extra
# pulls in `cryptography`, which PyJWT needs to verify ES256/RS256
# tokens — Supabase moved off the legacy HS256 shared secret to
# asymmetric signing keys (ECC P-256 by default) in 2024-2025.
PyJWT[crypto]==2.9.0
# Rate limiting middleware. Defaults are conservative; override
# per-route with @limiter.limit("X/minute") as needed (SOP §1.4).
slowapi==0.1.9
# Resend transactional email SDK — used by services/email.py to send
# form invitations and restaurant-share links.
resend==2.4.0
